<!-- Bouton pour afficher la fenêtre modale -->
<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#confirmationModal">
    Supprimer le transporteur
</button>

<!-- Fenêtre modale de confirmation -->
<div class="modal fade" id="confirmationModal" tabindex="-1" aria-labelledby="confirmationModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="confirmationModalLabel">Confirmation de suppression</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Êtes-vous sûr(e) de vouloir supprimer le transporteur "{{ carrier.name }}" ?
            </div>
            <div class="modal-footer">
                <!-- Formulaire pour soumettre la suppression -->
                <form method="post" action="{{ path('app_carrier_delete', {'id': carrier.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrier.id) }}">
                    <button type="submit" class="btn btn-danger">Confirmer la suppression</button>
                </form>
                <!-- Bouton pour fermer la fenêtre modale -->
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
            </div>
        </div>
    </div>
</div>

