<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Récapitulatif Commande</title>
    <style>
        body { font-family: Arial, sans-serif; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th { background-color: #f2f2f2; text-align: left; }
    </style>
</head>
<body>
    <h1>Récapitulatif de la Commande</h1>
    <p>Référence : {{ order.reference }}</p>
    <p>Total TTC : {{ order.total|number_format(2, '.', ',') }} €</p>

    <h2>Produits</h2>
    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Quantité</th>
                <th>Prix HT</th>
                <th>Tax</th>
                <th>Prix TTC</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for product in order.orderProduct %}
                <tr>
                    <td>{{ product.product.title }}</td>
                    <td>{{ product.quantity }}</td>
                    <td>{{ product.product.prixVenteHt|number_format(2, '.', ',') }} DT</td>
                    <td>{{ product.product.taxRules.name }}</td>
                    <td>{{ product.product.prixVenteTtc|number_format(2, '.', ',') }} DT</td>
                    <td>{{ (product.product.prixVenteTtc * product.quantity)|number_format(2, '.', ',') }} DT</td>
                </tr>
            {% endfor %}
            <tr>
                <td colspan="5"><strong>Livraison</strong></td>
                <td>{{ order.carrierPrice.price|number_format(2, '.', ',') }} €</td>
            </tr>
            <tr>
                <td colspan="5"><strong>Total TTC</strong></td>
                <td>{{ order.total|number_format(2, '.', ',') }} €</td>
            </tr>
        </tbody>
    </table>
</body>
</html>
