<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
    </style>
</head>
<body>
    <h1>Facture</h1>
    <p><strong>Commande :</strong> {{ order.reference }}</p>
    <p><strong>Date :</strong> {{ order.createdAt|date('d/m/Y') }}</p>
    <p><strong>Client :</strong> {{ order.client.firstname }} {{ order.client.lastname }}</p>
    <p><strong>Adresse de livraison :</strong> {{ order.address }}</p>
    <p><strong>Ville :</strong> {{ order.carrierPrice.city.value }}</p>

    <h2>Détails de la commande</h2>
    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Quantité</th>
                <th>Prix unitaire</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for orderProduct in order.orderProduct %}
                <tr>
                    <td>{{ orderProduct.product.title }}</td>
                    <td>{{ orderProduct.quantity }}</td>
                    <td>{{ orderProduct.product.prixVenteTtc|number_format(2, '.', ',') }} €</td>
                    <td>{{ (orderProduct.quantity * orderProduct.product.prixVenteTtc)|number_format(2, '.', ',') }} €</td>
                </tr>
            {% endfor %}
            <tr>
                <td colspan="3"><strong>Livraison</strong></td>
                <td>{{ order.carrierPrice.price|number_format(2, '.', ',') }} €</td>
            </tr>
            <tr>
                <td colspan="3"><strong>Total</strong></td>
                <td>{{ order.total|number_format(2, '.', ',') }} €</td>
            </tr>
        </tbody>
    </table>
</body>
</html>