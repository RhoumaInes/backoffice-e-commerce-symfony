{% extends 'base.html.twig' %}

{% block title %}Taxes{% endblock %}

{% block body %}
<div class="row">
    <!-- left column -->
    <div class="col-md-10">
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title">Taxes</h3>
            </div>
            <div class="card-body">
                {% if form_errors(form) %}
                    <div class="alert alert-danger">{{ form_errors(form) }}</div>
                {% endif %} 
                {{ include('tax_rules/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
            </div>
        </div>
        {{ include('tax_rules/_delete_form.html.twig') }}
        <div><a href="{{ path('app_tax_rules_index') }}">Retour à la liste</a></div>
    </div>  
</div>
{% endblock %}